name: bash
display_name: Bash
versions: ["22.04", "20.04"]

detection:
  files: ["*.sh", "script.sh", "main.sh"]

ports: []

files:
  dockerfile: Dockerfile.template
  scaffolding: [".gitignore", "main.sh"]